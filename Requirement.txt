streamlit==1.25.0  # Latest version; adjust if incompatible
newspaper3k==0.2.8
langchain==0.0.273  # Ensure compatibility with used modules
transformers==4.35.0  # For pipeline and HuggingFace integrations
torch==2.0.1  # Specify version for GPU/CPU compatibility
nltk==3.9.0  # Version with 'punkt' tokenizer
faiss-cpu==1.7.4  # For FAISS vector store (use `faiss-gpu` if on GPU)